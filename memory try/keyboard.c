int pos;
array out[];

//red = button1 ~       up. Enter~ Numbers 
//red = button2 ~         A. ~ caps (toggle)
//red = button3 ~         B. ~ specal charicters.
//gre = button4 ~         C. ~ Enter.
//gre = button5 ~ Down. ~ Exit.


// screen 1 draw keyboard setup.
// draw keys based on pos.

if((digitalRead(button 1) == 1)
 // button 1
{
        dealy(250);
               if((digitalRead(button 1)  == 1); 
                          {
								while(digtalRead(button 1) == 1)
									{
                                 // long press
                                //Number mode
                                //swap pos?
									}
                           }
									 else
									{
										   // short press
										   pos = pos + 1;
									}

}  




if((digitalRead(button 2) == 1)
{
        dealy(60ms);
               if((digitalRead(button 2)  == 1); 
                          {
                                 // long press
                                 pos = ( po
                           }
         else
         {
               out[] = ((pos × 3) + 1);
               out[index] + 1;
          }

}  